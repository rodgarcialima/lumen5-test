import React from 'react';
import { render } from '@testing-library/react';
import App from './App';

describe('filterTodos', function() {
  // const todos = {
  //   'laundry': { category: 'home', done: false },
  //   'write report': { category: 'work', done: true },
  //   'vacuum': { category: 'home', done: false }
  // };

  // it('should return all elements if null category is passed', function() {
  //   const actualResult = filterTodos(todos, null);
  //   expect(actualResult).toEqual(todos);
  // });

  // it('should return all todos if no category is passed', function() {
  //     const actualResult = filterTodos(todos, '');
  //     expect(actualResult).toEqual(todos);
  // });

  // it('should return 2 elements if home category is passed', function() {
  //   const actualResult = filterTodos(todos, 'home');
  //   const expectedResult = {
  //     'laundry': { category: 'home', done: false },
  //     'vacuum': { category: 'home', done: false }
  //   };
  //   expect(actualResult).toEqual(expectedResult);
  // });

  // it('should return nothing if category does not exists', function() {
  //   const actualResult = filterTodos(todos, 'does not exists');
  //   const expectedResult = {};
  //   expect(actualResult).toEqual(expectedResult);
  // });
});

